apiVersion: perses.dev/v1alpha2
kind: Perses
metadata:
  name: perses
  namespace: observability-operator
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  storage:
    pvcTemplate:
      accessModes:
        - ReadWriteMany
