apiVersion: config.openshift.io/v1
kind: APIServer
metadata:
  name: cluster
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  servingCerts:
    namedCertificates:
      - names:
          - api.lab-01.okd.home.arpa
        servingCertificate:
          name: api-server-tls
